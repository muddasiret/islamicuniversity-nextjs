const PageTitle = ({ title }) => {
  return (
    <div>
      <h1 className="text-3xl text-primaryblue font-bold border-b-2 pb-1 border-slate-100">{title}</h1>
    </div>
  );
};

export default PageTitle;
